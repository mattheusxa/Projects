import 'package:flutter/material.dart';

class VakinhaTextformfield extends StatelessWidget {
  const VakinhaTextformfield({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
      ),
      body: Container(),
    );
  }
}
